import { PageContainer } from "@/components/PageContainer";

export function HealthPage() {
  return (
    <PageContainer
      title="Health"
      description="Placeholder — Sprint 5 will deliver the health summary banner, integration tiles, and inline stuck items list."
    />
  );
}
