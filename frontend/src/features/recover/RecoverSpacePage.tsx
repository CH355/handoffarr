import { PageContainer } from "@/components/PageContainer";

export function RecoverSpacePage() {
  return (
    <PageContainer
      title="Recover space"
      description="Placeholder — Sprint 3 will deliver the three-section recommendation pattern, inline confirmation, and success-with-Undo."
    />
  );
}
