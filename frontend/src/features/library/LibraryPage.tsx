import { PageContainer } from "@/components/PageContainer";

export function LibraryPage() {
  return (
    <PageContainer
      title="Library"
      description="Placeholder — Sprint 4 will deliver search, filter chips, sort, and the library item rows."
    />
  );
}
