import { PageContainer } from "@/components/PageContainer";

export function HomePage() {
  return (
    <PageContainer
      title="Home"
      description="Placeholder — Sprint 2 will deliver the calm baseline with the primary banner, stat tiles, and recently added list."
    />
  );
}
